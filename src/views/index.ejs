<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <title>Broadband and Standardized Testing by State</title>
  <meta name="author" content="You">
  <meta name="description">
  <meta content="text/html" http-equiv="Content-type" />
  <meta content="width=device-width, initial-scale=1" name="viewport" />
  <link rel="stylesheet" href="../styles.css">
</head>

<body>
  <header>
    <h1></h1>
  </header>
  <section id="charcter-table">
    <table>
      <tr>
        <th>Name</th>
        <th>Gender</th>
        <th>House</th>
        <th>Species</th>
        <th>Skill</th>
      </tr>
      <% for(character in data){ %>
      <tr>
        <td><a href="<%=data[character].link%>.html"><%= character %></a></td>
        <td><%= data[character].gender %></td>
        <td><%= data[character].house %></td>
        <td><%= data[character].species %></td>
        <td>
          <% if (data[character].skills.length > 0){ %>
          <ol>
            <% data[character].skills.forEach(function(val){%>
            <li> <%=val%></li>
            <%}) %>
          </ol>
          <%}%>
        </td>
      </tr>
      <%}%>
    </table>
  </section>

  <%- include ('global/footer');  %>

</body>

</html>
